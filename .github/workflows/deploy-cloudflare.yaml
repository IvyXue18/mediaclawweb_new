name: Deploy to Cloudflare Workers

on:
  push:
    branches: ['main']
  workflow_dispatch:

concurrency:
  group: deploy-cloudflare
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create .env.production
        env:
          ENV_PRODUCTION: ${{ secrets.ENV_PRODUCTION }}
        if: ${{ env.ENV_PRODUCTION != '' }}
        run: echo "$ENV_PRODUCTION" > .env.production

      - name: Create Wrangler config
        run: cp wrangler.production.jsonc wrangler.jsonc

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build (Cloudflare preset)
        run: |
          set -a
          [ -f .env.production ] && . ./.env.production
          set +a
          pnpm cf:build
          node scripts/create-wrangler-deploy-config.mjs production

      - name: Deploy with Wrangler
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: pnpm exec wrangler deploy --config .wrangler/production-wrangler.json

      - name: Submit URLs to IndexNow
        continue-on-error: true
        run: pnpm indexnow:submit
